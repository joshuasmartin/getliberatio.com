.alert.alert-warning.alert-dismissable
  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
  %p <strong>Liberatio Agent only supports Windows at this time</strong>
  %p
    #{link_to "Get Notified", browse_roadmap_path} when we add other systems,
    and keep an eye on our #{link_to "Roadmap", browse_roadmap_path}.
    <strong>But keep in mind</strong>, you can add devices of any type, manually without using the Agent.

%ol.breadcrumb
  %li.active
    Devices

.row
  .col-md-10
    %table.table.table-bordered.table-striped.table-condensed
      %thead
        %tr
          %th.col-md-2 Name
          %th.col-md-2 Role
          %th Operating System
          %th Location
          %th.col-md-2 Model #
          %th

      %tbody
        - if @nodes.any?
          - @nodes.each do |node|
            - if !(node.is_managed? && node.inventoried_at.blank?)
              %tr
                %td #{node.name}
                %td
                  = image_tag("#{node.role}.png".downcase, :alt => node.role, :style => "width: 16px;")
                  = node.role
                %td #{node.operating_system}
                %td #{node.location}
                %td #{node.model_number}
                %td #{link_to 'Details', node_path(node)}
        - else
          %tr
            %td{ colspan: 6 }
              Oh no! You haven't added any devices yet -
              = link_to "Add One Now", "#"
              / TODO add image of oh no or something. have it link to a modal that displays instructions

  .col-md-2
    = link_to "Add Device", new_node_path, :class => "btn btn-primary btn-block"

    %br/

    .alert.alert-info
      Registration Code
      %br/
      %strong
        = current_user.organization.registration_code