<h1>Listing notes</h1>

<table>
  <thead>
    <tr>
      <th>Node</th>
      <th>Organization</th>
      <th>Title</th>
      <th>Contents</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @notes.each do |note| %>
      <tr>
        <td><%= note.node_id %></td>
        <td><%= note.organization_id %></td>
        <td><%= note.title %></td>
        <td><%= note.contents %></td>
        <td><%= note.user_id %></td>
        <td><%= link_to 'Show', note %></td>
        <td><%= link_to 'Edit', edit_note_path(note) %></td>
        <td><%= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Note', new_note_path %>
